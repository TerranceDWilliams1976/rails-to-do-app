
<table class="table table-bordered">
  <tbody>
    <% @lists.each do |list| %>
      <% if list.completed %>
      
        <tr class= 'table-primary'>
          <td class="striker"><%= link_to list.description, edit_list_path(list) %><% list.description %></td>
          <td>
            <%= form_for(list) do |f| %>
              <%= f.text_field :description, type: 'hidden' %>
              <%= f.check_box :completed, :checked => false, :style => 'visibility: hidden' %>
              <%= f.submit "UnCross", class: 'btn btn-outline-secondary' %>
            <% end %>
          
          </td>
          <td>
            <% link_to 'Edit', edit_list_path(list) %> 
           <center><%= link_to 'Delete', list, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-danger' %></center>
          </td>
        </tr>
      <% else %>
        <tr>
          <td><%= link_to list.description, edit_list_path(list) %><% list.description %></td>
          <td>
            <%= form_for(list) do |f| %>
              <%= f.text_field :description, type: 'hidden' %>
              <%= f.check_box :completed, :checked => true, :style => 'visibility: hidden' %>
              <%= f.submit "Cross Off", class: 'btn btn-outline-success' %>
            <% end %>
          
          
          </td>
          <td>
            <% link_to 'Edit', edit_list_path(list) %> 
           <center><%= link_to 'Delete', list, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-danger' %></center>
          </td>
        </tr>
      <% end %>
      
    <% end %>
  </tbody>
</table>

<br>

<% link_to 'New List', new_list_path, class: 'btn btn-success' %>

<br/>
<br/>

<%= link_to 'Delete Marked Items', home_trash_path, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-info' %><br/><br/>
<%= link_to 'Delete All Items', home_trash_all_path, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-warning' %>